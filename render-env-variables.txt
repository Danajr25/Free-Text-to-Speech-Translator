# This file contains environment variables needed for the Render deployment
# Copy these to the Render dashboard environment variables section

# Database Configuration
DB_HOST=your-db-host.render.com
DB_PORT=5432
DB_DATABASE=speech_translator_db
DB_USERNAME=speech_translator_user
DB_PASSWORD=your_secure_password

# App Settings
APP_KEY=base64:YOUR_GENERATED_KEY
APP_ENV=production
APP_DEBUG=false

# Redis (if needed)
# REDIS_HOST=your-redis-host.render.com
# REDIS_PASSWORD=your_redis_password
# REDIS_PORT=6379

# Other app-specific settings
# Add any translation API keys or other environment variables here